# YMShop (Node.js + Express + MySQL)

## วิธีติดตั้ง
1) ติดตั้ง Node.js (แนะนำ v18 LTS)
2) สร้างฐานข้อมูลโดยรันไฟล์ `db/schema.sql` ใน MySQL Workbench
3) ตั้งค่า `.env`
4) รันคำสั่ง
```bash
npm install
npm start
