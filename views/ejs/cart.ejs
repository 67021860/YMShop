<div class="page">
  <div class="card glass">
    <div class="cart-header">
      <div>
        <h1 class="title">ตะกร้าสินค้า</h1>
        <div class="muted small">ติ๊กเลือกสินค้าที่ต้องการชำระเงิน ระบบจะคิดยอดเฉพาะที่เลือก</div>
      </div>

      <div class="sum-pill">
        <div class="muted small">รวมที่เลือก</div>
        <div class="sum-amount"><span id="totalText">0.00</span> บาท</div>
      </div>
    </div>

    <% if (!items || items.length === 0) { %>
      <div class="empty">
        <div class="empty-emoji">🛒</div>
        <p>ยังไม่มีสินค้าในตะกร้า</p>
        <a class="btn primary" href="/categories">ไปเลือกสินค้า</a>
      </div>
    <% } else { %>

      <div class="cart-actions">
        <label class="check-all">
          <input type="checkbox" id="checkAll">
          เลือกทั้งหมด
        </label>
      </div>

      <div class="cart-list" id="cartList">
        <% items.forEach(it => { %>
          <div class="cart-item"
               data-cart-id="<%= it.cart_id %>"
               data-price="<%= Number(it.price) %>">

            <div class="ci-left">
              <input class="selectItem" type="checkbox" />
            </div>

            <div class="ci-img">
              <img src="<%= it.image_url %>" alt="<%= it.name %>">
            </div>

            <div class="ci-info">
              <div class="ci-name"><%= it.name %></div>
              <div class="ci-meta">
                <span class="cart-badge">ราคา <b><%= Number(it.price).toFixed(2) %></b> บาท</span>
              </div>
            </div>

            <div class="ci-qty">
              <button class="btn-icon minus" type="button">−</button>
              <span class="qtyText"><%= it.qty %></span>
              <button class="btn-icon plus" type="button">+</button>
            </div>
          </div>
        <% }) %>
      </div>

      <form id="checkoutForm" method="POST" action="/checkout" class="checkout-bar">
        <input type="hidden" name="selectedIds" id="selectedIds" value="">
        <button class="btn primary" type="submit" id="payBtn" disabled>ชำระเงิน</button>
      </form>

    <% } %>
  </div>
</div>
